package com.studiversity.feature.room

import com.studiversity.database.table.Rooms
import com.studiversity.feature.room.model.CreateRoomRequest
import org.jetbrains.exposed.sql.insert

class RoomRepository {

    fun add(createRoomRequest: CreateRoomRequest) {
        Rooms
    }
}